# AGENTS.md

## Scope

默认语言: TypeScript、Vue、Markdown

允许修改目录:

- `docs/` - 文档源文件
- `config/.vitepress/` - VitePress 配置和主题定制
- `build/` - 构建部署脚本
- `public/` - 静态资源文件
- 根目录配置文件（package.json、tsconfig.json）

禁止修改目录:

- `node_modules/`
- `dist/`
- `.git/`

## Quality Gate

改动后必须执行:

1. 判断是否需要更新以下文档，如需要则按对应原则更新：

   - **AGENTS.md**（面向AI编程大模型）：
     - 当项目范围、目录结构、规范、命令、提交规则、注意事项等发生变化时需要更新
     - 更新原则：只包含AI需要的指令和规范，移除面向人类读者的冗余说明文字，保持简洁清晰

   - **README.md**（面向图书阅读者）：
     - 当项目介绍、核心内容、特色亮点、联系方式等发生变化时需要更新
     - 更新原则：对普通读者友好，不包含技术性配置和命令（如版本号、构建命令、开发步骤等），不包含对普通读者无意义的内容（如技术栈、致谢开源项目等），保留核心内容

   - **避免重复原则**：
     - 单文档内部：AGENTS.md 或 README.md 各自内部不要出现重复章节或内容
     - 跨文档之间：AGENTS.md 和 README.md 之间也不应该出现重复内容，面向不同受众应有明确分工

2. `npm run lint` - 执行完整检查

## Delivery Format

- 文档修改需包含清晰的修改说明
- 文件引用格式: `文件路径`（如 `config/.vitepress/config.mts`）

## Project Structure

```text
docs/                          # 项目根目录
├── docs/                      # 文档源目录
│   ├── index.md               # 首页
│   ├── about.md               # 关于页面
│   ├── 01-vibe-working/       # AI辅助工作指南
│   ├── 02-vibe-coding/        # AI辅助编程指南
│   ├── 03-fullstack/          # 全栈开发指南
│   ├── 04-system-refactor/    # 重构你的系统
│   ├── 05-health-handbook/    # 健康科普手册
│   └── 06-listening-world/    # 倾听世界
├── config/.vitepress/         # VitePress 配置
│   ├── config.mts             # 主配置
│   ├── sidebar.ts             # 侧边栏和导航生成逻辑
│   └── theme/                 # 主题定制
├── build/
├── public/
├── package.json
└── tsconfig.json
```

## 写作规范

### 书籍文档命名规范

#### 术语定义

| 术语 | 定义 | 识别方式 |
|------|------|---------|
| 书籍根目录 | 书籍最顶层目录 | 目录内包含 `toc.md` |
| 书籍子目录 | 书籍根目录下所有层级子目录 | 位于书籍根目录树内 |
| 书籍类文档 | 整个书籍目录树 | 书籍根目录 + 所有层级子目录 |

#### 书籍根目录文件

| 文件 | 命名格式 | 侧边栏位置 |
|------|---------|-----------|
| 书籍首页 | `index.md` | 第1位，显示为"前言" |
| 目录页 | `toc.md` | 第2位，显示为"目录" |
| 附录/术语表 | `99-xxx.md` | 按序号排在所有章节之后 |
| 其他独立文件 | `NN-xxx.md` | 与子目录统一排序 |

排序逻辑: 前言 → 目录 → 所有文件/子目录统一按序号排序（不区分类型，仅看序号前缀）

#### 篇目录（书籍根目录下第一层级子目录）

命名格式: `NN-篇英文名/`，序号 01~89
目录内必须包含: `index.md`

排序逻辑: 先独立文件（按序号），后子目录（按序号）

#### 章目录（篇目录下的子目录）

命名格式: `NN-章英文名/`

排序逻辑: 同篇目录

#### 节文件（章目录下的 Markdown 文件）

命名格式: `NN-节英文名.md`

#### 排序规则总结

| 目录层级 | 排序规则 |
|---------|---------|
| 书籍根目录 | 前言 → 目录 → 所有文件/子目录统一按序号排序 |
| 书籍子目录（篇/章） | 先独立文件（按序号），后子目录（按序号） |
| 普通文档目录 | 同书籍子目录规则 |

仅在书籍根目录层级，独立文件与子目录混合排序，仅比较序号前缀。其他所有层级目录均按"先文件，后目录"排序。

#### 忽略规则

名称以 `_` 开头的文件和文件夹会被自动忽略：

- 不显示在侧边栏
- 不显示在导航栏
- 不纳入排序处理

### Frontmatter 撰写规范

| 文件类型 | 必需字段 | 可选字段 | 说明 |
|---------|---------|---------|------|
| docs 根目录下的 .md 文件（非 index.md） | 无 | title | - |
| 第一层子目录的 index.md | shortTitle | title | 文件夹名需以两位数字序号开头 |
| 书籍根目录的 index.md | shortTitle | title | 侧边栏显示为"前言" |
| 书籍 toc.md | 无 | 无 | 侧边栏显示为"目录" |
| 篇目录的 index.md | 无 | title | title 为该篇的显示名称 |
| 章目录的 index.md | 无 | title | title 为该章的显示名称 |
| 节文件（.md） | 无 | title | title 为该节的显示名称 |
| 其他目录的 index.md | 无 | title | - |
| 其他 .md 文件 | 无 | title | - |

字段说明:

- `title`: 侧边栏显示标题，未填写时从文档一级标题或文件名自动提取
- `shortTitle`: 导航栏短标题（仅第一层子目录 index.md 需要）
- `sidebar`: 设为 `false` 可隐藏侧边栏显示

文件夹命名规范:

- 第一层子目录: 文件夹名以两位数字序号开头，格式 `NN-name`
- 序号作用: 决定板块在导航栏的显示顺序，从 01 开始递增

空 frontmatter（无任何字段）会被自动移除。

### 内容风格

1. 语言: 中文为主，技术术语保留英文
2. 语气: 专业、友好、实用
3. 结构: 使用清晰的标题层级（##、###）
4. 链接: 外部资源必须提供完整 URL
5. 禁止使用 `---` 水平分隔线：标题层级已足够划分章节，分隔线影响阅读体验

### 易过时信息处理原则

#### 避免在图书内容中包含以下易过时信息

- 政府部门、机构的具体地址、联系电话
- 特定版本号、价格信息（除非是历史数据或明确标注时间戳）
- 临时政策细节（可能随时间变化的具体规定）

#### 处理方式

1. **优先提供官方渠道信息**：官网链接、统一服务热线等
2. **引导读者查询最新信息**：说明信息可能变化，建议查询官方渠道
3. **明确标注信息获取方式**：告诉读者如何获取最新信息
4. **保留时间戳**：如必须包含时效性信息，应明确标注发布时间或有效期

### 图书内容质量要求

#### 内容准确性

- 所有技术信息、代码示例、数据必须经过验证
- 注明内容的适用版本或时间范围，及时更新过时信息
- 引用外部资料时提供可靠来源链接

#### 内容深度

- 每个章节应有独特的见解和实践经验，而非泛泛而谈
- 不仅介绍"是什么"，更要解释"为什么"和"怎么用"
- 包含实际使用场景、完整案例、可操作的步骤
- 指出常见错误和注意事项

#### 内容广度

- 覆盖主题的主要方面，不遗漏重要知识点
- 从入门到进阶，适应不同水平的读者
- 及时纳入新兴工具和热门产品（如 DeepSeek、Cursor 等）

#### 可读性

- 避免病句、歧义，保持段落连贯
- 合理使用标题、列表、表格、代码块
- 复杂内容应配合截图、流程图或 GIF 演示
- 代码块必须指定语言标识

#### 实用性

- 读者能根据文档直接执行操作
- 提供可复用的模板、脚本、配置
- 包含提升效率的技巧和最佳实践

#### 禁止事项

- 大段复制粘贴官方文档而不加工
- 过时或已被淘汰的工具/方法作为主流推荐
- 未经测试的代码示例
- 侵犯版权的内容
- 主观臆测或未经验证的技术细节

## 可用命令

```bash
npm run docs:dev      # 本地开发预览
npm run lint          # 执行完整检查
npm run buildAndDeploy # 检查并构建部署
```

## 提交规范

提交信息格式: `<type>(<scope>): <subject>`

- `<subject>` 需为中文（专业词汇、文件名等特殊场景除外）

示例:

- `docs(板块名): 描述` - 文档更新
- `config: 描述` - 配置修改
- `theme: 描述` - 主题定制

## 注意事项

- 侧边栏由目录结构自动生成（通过 `config/.vitepress/sidebar.ts`）
- `docs/index.md` 作为首页，不进入侧边栏
- 新增板块需在目录下创建 `index.md` 文件才会出现在导航栏
