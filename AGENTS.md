# AGENTS.md

## 项目简介

- 本项目是一个基于 VitePress 的中文知识库/电子书站点，内容聚焦 AI 应用、编程实践、全栈开发、系统重构、健康科普与听力健康。
- 项目根目录包含 `package.json`，文档源位于 `docs/`，站点配置位于 `config/.vitepress/`，构建与部署脚本位于 `build/`。

## 范围

- 默认语言：TypeScript、Vue、Markdown，内容写作以中文为主，技术术语可保留英文。
- 允许修改目录：`docs/`、`config/.vitepress/`、`build/`、`public/`。
- 允许修改根目录配置文件：`package.json`、`package-lock.json`、`tsconfig.json`、`AGENTS.md`、`README.md` 以及与格式化、lint、构建相关的根目录配置文件。
- 禁止修改目录：`node_modules/`、`dist/`、`.git/`。
- 不主动修改 IDE、临时或第三方工具目录：`.idea/`、`.uploads/`、`.claude/`、`.codebuddy/`、`.trae/`，除非用户明确要求。

## 改动检查

**改动后必须执行：**

1. 判断是否需要更新 `AGENTS.md` 或 `README.md`：
   - `AGENTS.md` 面向 AI 编程大模型；当项目范围、目录结构、规范、命令、提交规则、注意事项发生变化时更新。
   - `README.md` 面向图书阅读者；当项目介绍、核心内容、特色亮点、联系方式发生变化时更新。
   - 避免单文档内部重复，也避免 `AGENTS.md` 与 `README.md` 跨文档重复；两者受众和职责必须清晰区分。
2. 执行完整检查：`npm run lint`。

**相关检查命令：**

```bash
npm run lint
npm run lint:code
npm run lint:markdown
npm run check:type
npm run docs:build
```

**检查项：**

- Markdown 结构、Frontmatter、标题层级、脚注引用、代码块语言标识符合规范。
- TypeScript、Vue、VitePress 配置通过格式化、类型检查、ESLint 和构建。
- 医学、健康、政策等专业内容具备权威来源，不包含未验证或易过时的细节。

## 交付格式

- 文档修改需包含清晰的修改说明、检查结果和是否更新 `README.md`/`AGENTS.md` 的判断。
- 文件引用使用项目相对路径，并按当前执行环境要求在需要时补充行号，例如 `config/.vitepress/config.mts:1`。
- 若检查失败，说明失败命令、失败原因、是否与本次改动相关，以及建议下一步。
- 不输出大段文件全文；优先概括关键改动并引用文件路径。

## 交互确认格式

- 当需要用户确认、取消或从多个选项中选择时，必须用编号列表给出选项，并说明每个编号的含义。
- 用户可只回复编号完成选择，不要求输入完整选项文案。
- 二元对立选项默认使用 `1` 表示肯定意义的选项，使用 `0` 表示否定意义的选项。
- 示例：`1. 确认提交`、`0. 暂不提交`。

## 项目结构

```text
docs/                          # 项目根目录
├── docs/                      # 文档源目录
│   ├── index.md               # 首页，不进入侧边栏
│   ├── about.md               # 关于页面
│   ├── 01-vibe-working/       # AI 辅助工作指南
│   ├── 02-vibe-coding/        # AI 辅助编程指南
│   ├── 03-fullstack/          # 全栈开发指南
│   ├── 04-system-refactor/    # 重构你的系统
│   ├── 05-health-handbook/    # 健康科普手册
│   └── 06-listening-world/    # 倾听世界
├── config/.vitepress/         # VitePress 配置与主题定制
│   ├── config.mts             # 主配置
│   ├── sidebar.ts             # 侧边栏和导航生成逻辑
│   └── theme/                 # 自定义主题、组件、样式
├── build/                     # 构建、部署、Frontmatter 校验脚本
├── public/                    # 静态资源文件
├── package.json               # npm 脚本与依赖配置
└── tsconfig.json              # TypeScript 配置
```

- 侧边栏由目录结构自动生成，核心逻辑位于 `config/.vitepress/sidebar.ts`。
- 新增一级板块需在 `docs/NN-name/index.md` 中提供 `shortTitle`，否则不会正常出现在顶部导航。
- 名称以 `_` 开头的文件或文件夹会被自动忽略，不显示在导航栏和侧边栏，也不参与排序。

## AI 能力模型

### 行动前思考

- **假设显式化**：开始前说明关键假设；涉及最新事实、医学、政策、工具版本时必须核验来源。
- **决策点显式化**：遇到范围、结构、命名、是否新增文件等关键分歧时，先判断能否基于现有规范决策；无法判断时向用户确认。
- **目标驱动执行**：始终围绕用户目标和当前项目规范行动，不因顺手发现问题而扩大范围。

### 克制与精简

- **简单优先**：优先采用最小、清晰、可维护的修改，不引入不必要的依赖、抽象或目录变化。
- **精确修改**：只修改与任务直接相关的文件；保留已有内容和风格，避免无关重排。
- **显式分级筛选**：区分必须修复、建议优化、暂不处理的问题，避免把建议项当成必做项。

### 验证与比对

- **交叉比对式矛盾定位**：对照用户要求、`AGENTS.md`、项目实际结构和脚本输出定位冲突。
- **逆向逻辑验证**：从最终交付反推是否满足命名、Frontmatter、引用、检查命令和读者体验要求。
- **结果可追溯**：交付时说明改动文件、验证命令和剩余风险，确保用户能复查。

## 路径格式规范

- 文档和回复中的项目路径统一使用正斜杠 `/`，例如 `config/.vitepress/sidebar.ts`。
- 路径包含空格时使用引号包裹，例如 `"docs/07-new book/index.md"`。
- 终端命令中的路径应按当前 shell 正确转义；PowerShell 中优先使用 `-LiteralPath` 处理固定路径。
- 引用文件位置时使用项目相对路径；需要精确定位时使用 `路径:行号`，例如 `docs/05-health-handbook/index.md:1`。
- 不使用 `file://`、`vscode://` 等 URI 作为文件引用。

## 终端命令能力识别

- 默认在项目根目录 `C:/workspace/develop/docs` 执行命令。
- 当前环境优先使用 PowerShell Core 命令形式：`pwsh -Command "..."`。
- 搜索文件优先使用 `rg --files`，搜索文本优先使用 `rg`；若不可用再使用 PowerShell 原生命令。
- 读取文件使用 `Get-Content -Raw -LiteralPath`，列目录使用 `Get-ChildItem -Force`。
- 禁止对 `node_modules/`、`dist/`、`.git/` 执行写入、删除或格式化操作。

## 需要遵守的规则

### 书籍文档命名规范

#### 术语定义

| 术语 | 定义 | 识别方式 |
|------|------|---------|
| 书籍根目录 | 书籍最顶层目录 | 目录内包含 `toc.md` |
| 书籍子目录 | 书籍根目录下所有层级子目录 | 位于书籍根目录树内 |
| 书籍类文档 | 整个书籍目录树 | 书籍根目录 + 所有层级子目录 |

#### 书籍根目录文件

| 文件 | 命名格式 | 侧边栏位置 |
|------|---------|-----------|
| 书籍首页 | `index.md` | 第 1 位，显示为“前言” |
| 目录页 | `toc.md` | 第 2 位，显示为“目录” |
| 附录/术语表 | `99-xxx.md` | 按序号排在所有章节之后 |
| 其他独立文件 | `NN-xxx.md` | 与子目录统一排序 |

- 书籍根目录排序：前言 → 目录 → 所有文件/子目录统一按序号排序，不区分类型，仅比较序号前缀。
- 篇目录命名格式：`NN-篇英文名/`，序号 `01` 至 `89`，目录内必须包含 `index.md`。
- 章目录命名格式：`NN-章英文名/`。
- 节文件命名格式：`NN-节英文名.md`。
- 书籍子目录和普通文档目录排序：先独立文件按序号排序，再子目录按序号排序。
- 仅书籍根目录允许独立文件与子目录混合排序；其他层级均按“先文件，后目录”排序。

### Frontmatter 撰写规范

| 文件类型 | 必需字段 | 可选字段 | 说明 |
|---------|---------|---------|------|
| `docs/` 根目录下的 `.md` 文件（非 `index.md`） | 无 | `title` | - |
| 第一层子目录的 `index.md` | `shortTitle` | `title` | 用于导航栏短标题，文件夹名需以两位数字序号开头 |
| 书籍根目录的 `index.md` | 无 | `title` | 侧边栏显示为“前言” |
| 书籍 `toc.md` | 无 | 无 | 侧边栏显示为“目录” |
| 篇目录的 `index.md` | 无 | `title` | `title` 为该篇的显示名称 |
| 章目录的 `index.md` | 无 | `title` | `title` 为该章的显示名称 |
| 节文件（`.md`） | 无 | `title` | `title` 为该节的显示名称 |
| 其他目录的 `index.md` | 无 | `title` | - |
| 其他 `.md` 文件 | 无 | `title` | - |

- `title` 用于侧边栏显示；未填写时从文档一级标题或文件名自动提取。
- `shortTitle` 仅 `docs/` 第一层子目录的 `index.md` 需要，用于顶部导航栏短标题。
- `sidebar: false` 可隐藏侧边栏显示。
- 第一层子目录命名格式为 `NN-name`，序号决定导航栏顺序。
- 空 frontmatter 会被自动移除。

### 内容风格

- 中文为主，技术术语保留英文。
- 语气专业、友好、实用。
- 使用清晰标题层级，优先使用 `##`、`###`。
- 外部资源必须提供完整 URL。
- 禁止使用 `---` 水平分隔线。
- 代码块必须指定语言标识。

### 参考资料规范

- 撰写医学、健康、政策等专业内容时，必须引用权威来源。
- 推荐来源：WHO、NIH、NIDCD、CDC、Mayo Clinic、Cleveland Clinic、UpToDate、中华医学会、国家卫健委、中国残联、国内三甲医院官方科普、PubMed、知网等。
- 优先选择科研/科普向来源，避免商业推广内容；如有中文权威来源，优先使用中文来源。
- 正文引用使用脚注格式 `[^N]`，参考资料章节使用 `[^N]: [标题](链接)`。
- 链接应指向具体页面，不指向泛化首页。
- 引用必须与正文内容紧密关联，避免引用无法支撑正文结论。

### 易过时信息处理原则

- 避免在图书内容中包含政府部门或机构的具体地址、联系电话、临时政策细节、价格和版本号等易过时信息。
- 优先提供官网链接、统一服务热线等官方渠道。
- 引导读者查询最新信息，并明确说明信息可能变化。
- 必须包含时效性信息时，标注发布时间或有效期。

### 图书内容质量要求

- 内容准确：技术信息、代码示例和数据必须经过验证；引用外部资料时提供可靠来源。
- 内容有深度：不仅说明“是什么”，还要解释“为什么”和“怎么用”，并提供实际场景、案例、步骤和注意事项。
- 内容有广度：覆盖主题主要方面，兼顾入门到进阶，及时纳入新兴工具和热门产品。
- 可读性良好：避免病句和歧义，合理使用标题、列表、表格、代码块、截图、流程图或 GIF。
- 实用性明确：读者能根据文档直接执行操作，优先提供可复用模板、脚本、配置和最佳实践。
- 禁止大段复制粘贴官方文档而不加工，禁止把过时或已淘汰工具作为主流推荐，禁止未经测试的代码示例、侵权内容、主观臆测或未经验证的技术细节。

### 可用命令

```bash
npm run docs:dev       # 本地开发预览
npm run lint           # 执行完整检查
npm run buildAndDeploy # 检查并构建部署
```

## 关键参考

- `README.md`：面向读者的项目介绍和核心内容说明。
- `config/.vitepress/config.mts`：VitePress 站点主配置。
- `config/.vitepress/sidebar.ts`：导航和侧边栏自动生成逻辑。
- `build/validate-frontmatter.ts`：Frontmatter 校验逻辑。
- `package.json`：可用 npm 脚本、依赖和项目元信息。
